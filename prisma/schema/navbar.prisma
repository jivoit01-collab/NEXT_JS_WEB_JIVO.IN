// ==========================================
// NAVBAR — top navigation for the public site
// Managed from /admin/navbar (CRUD)
// Top-level links: Our Essence, Our Products, Jivo Media, Community
// Each link can have sub-links shown in a hover mega-dropdown.
// ==========================================

model NavLink {
  id        String       @id @default(cuid())
  title     String       // "Our Essence", "Our Products", "Jivo Media", "Community"
  href      String?      // Not used — parent links are dropdown triggers, not navigable
  sortOrder Int          @default(0)
  isVisible Boolean      @default(true)
  subLinks  NavSubLink[] // Dropdown sub-items shown on hover
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

  @@index([sortOrder])
}

model NavSubLink {
  id        String   @id @default(cuid())
  navLinkId String
  navLink   NavLink  @relation(fields: [navLinkId], references: [id], onDelete: Cascade)
  title     String   // "The Story / Journey", "Canola Oil", etc.
  href      String?   // "/our-essence/story", "/products/canola-oil", etc.
  // Optional grouping for a two-level mega-dropdown (e.g. Products →
  // "Healthy Oils" / "Beverages" / "Staples"). Null = a flat/ungrouped sub-link
  // (the current behaviour for Our Essence, Media, Community). Group order on
  // the public site follows the smallest sortOrder among a group's sub-links.
  group     String?
  sortOrder Int      @default(0)
  isVisible Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([navLinkId, sortOrder])
}

// Singleton — only ever one row (id = "default").
// Holds branding/config that applies to the whole navbar.
model NavbarSetting {
  id        String   @id @default("default")
  logoUrl   String?  // Uploaded brand logo shown on the left of the navbar
  logoAlt   String?  // Optional alt text override (defaults to SITE_NAME)
  updatedAt DateTime @updatedAt
}
