// Intentionally empty.
//
// Import UI primitives directly from their leaf modules, for example:
//   import { Button } from '@/components/ui/button';
//
// Keeping this barrel empty prevents broad Radix/client primitive leakage into
// public route manifests when a public page only needs a single lightweight UI
// component.
export {};
