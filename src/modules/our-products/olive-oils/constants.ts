/**
 * Palette for the Olive Oils page — exact values from the approved design
 * screenshots. Kept in one place so every section stays consistent.
 */

/** Section 1 — hero background, olive green. */
export const OLIVE_HERO = '#697727';

/** Section 2 — "Extra Virgin" background, warm olive. */
export const OLIVE_VIRGIN = '#8C7D19';

/** Section 3 — "Extra Light" background, deep forest olive. */
export const OLIVE_LIGHT = '#475631';

/** Section 4 — "Pomace" background, muted khaki. */
export const OLIVE_POMACE = '#74734E';

/** Card background used across all three variant sections. */
export const OLIVE_CARD = '#9CA258';

/** Variant label under each card — pale sage. */
export const OLIVE_LABEL = '#D9E09E';
