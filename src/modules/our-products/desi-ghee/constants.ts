/**
 * Palette sampled from the approved Desi Ghee design screenshots.
 * Kept in one place so every section stays colour-consistent.
 */

/** Hero background — vivid grass green. */
export const GHEE_GREEN = '#1FA063';

/** Range section background — lighter leaf green. */
export const GHEE_LEAF = '#8AC75C';

/** Deep forest green — range cards, and copy set on the pale artwork. */
export const GHEE_FOREST = '#1FA063';

/** Deep forest green — range cards, and copy set on the pale artwork. */
export const GHEE_FOREST_BLACK = '#154540';

/** Card label gold, used under each jar in the range grid. */
export const GHEE_LABEL = '#8AC75C';


export const GHEE_FOREST_BG = '#154540';
