/**
 * Palette sampled from the approved Canola Oils design screenshots.
 * Kept in one place so every section stays colour-consistent.
 */

/** Hero deep green — exact value from the UI/UX design. */
export const CANOLA_GREEN = '#007363';

/** "What is canola" section background — dark forest green. */
export const CANOLA_FOREST = '#215338';

/** "What is canola" copy colour — soft sage, set on the forest field. */
export const CANOLA_SAGE_TEXT = '#CBC995';

/** Range + cold-pressed section background — warm taupe. */
export const CANOLA_TAUPE = '#AC937B';

/** Card green used behind each bottle in the range grid. */
export const CANOLA_CARD_GREEN = '#1F5437';

/** Sage used for the variant label under each bottle card. */
export const CANOLA_SAGE = '#B2B887';

/** "Why cold-pressed" section background — deeper, browner taupe. */
export const CANOLA_MOCHA = '#8E7865';

/** Soft gold/cream used for headings and accent copy. */
export const CANOLA_CREAM = '#e8e4b8';
