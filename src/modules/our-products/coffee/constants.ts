/**
 * Palette for the Koffie page — exact values from the approved design
 * screenshot ("KOFFIE PAGE" swatch sheet):
 *   #4C2118  espresso   #582C12  roast
 *   #8C512D  caramel    #3D1F08  bean
 * Kept in one place so every section stays consistent.
 */

/** Hero background — deepest espresso brown. */
export const COFFEE_ESPRESSO = '#4C2118';

/** Range section background — roast brown. */
export const COFFEE_ROAST = '#582C12';

/**
 * Card background behind each jar in the range grid, and the field behind the
 * "Key Highlights" copy — mid caramel brown.
 */
export const COFFEE_CARAMEL = '#8C512D';

/** "Beyond Beans" section fallback field — darkest bean brown. */
export const COFFEE_BEAN = '#3D1F08';

/** Heading / label ink on every brown field in the design. */
export const COFFEE_CREAM = '#FFFFFF';

/** Accent used for the range heading's hover underline. */
export const COFFEE_CREAM_ALT = '#E8C9A0';
