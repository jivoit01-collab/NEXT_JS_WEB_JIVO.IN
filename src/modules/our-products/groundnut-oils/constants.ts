/**
 * Palette for the Groundnut Oils page — exact values from the approved
 * design screenshots. Kept in one place so every section stays consistent.
 */

/** Hero background — deep roasted brown. */
export const GROUNDNUT_BROWN = '#603A1F';

/** Range section background — lighter warm brown. */
export const GROUNDNUT_RANGE_BROWN = '#72533E';

/** Card background behind each bottle in the range grid — groundnut yellow. */
export const GROUNDNUT_CARD_YELLOW = '#F9E65F';

/** "The goodness within" section background — mid amber brown. */
export const GROUNDNUT_AMBER = '#8C552F';

/** Heading colour on the amber field — pale wheat. */
export const GROUNDNUT_WHEAT = '#F8EBA2';

/**
 * Variant label under each range card. The cards are yellow, so the canola
 * page's pale sage would be unreadable here — this is the deep maroon from
 * the bottle labels in the design.
 */
export const GROUNDNUT_LABEL = '#7A1F2B';
